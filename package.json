{
  "name": "circles-app",
  "version": "1.0.0",
  "description": "Circles — a standalone social-listening app built on top of Spotify (real OAuth + backend + built-in SQLite).",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server/index.js",
    "dev": "node --watch --disable-warning=ExperimentalWarning server/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
